version: '3.8'

services:
  nginx-proxy:
    image: nginx:alpine
    container_name: nginx_same_origin_test
    network_mode: host
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped

